
version 0.7

2020/7/5(日)

version0.6をさらに変更した
hard_clusteringで返り値を返すようにして、
testerでその後の処理(クラスタリングの結果でユーザごとのメッセージを分ける)
その時に復号した結果の信号が必要だから、runから取ってきた。
また、その復号の性能を見るためにinput_bits(送信語)が必要だらか、これもrunから取ってきた。

この二つの値を使ってBERがわかる
ただ、送信時の通信路 h がある程度離れていないと、クラスタリングがちょうど均等にされず、あるユーザの信号が多くなってしまったりする。

また、ここから何と比べれば良いのか少し疑問。

2020/7/6(月)

tester.mでmesg(message signal)とrec(recieve signal)をユーザごとに設定。
そして、compare_bitsで通信成功確率を出した。
ただ、compare_bitsはinputとoutputで同じ長さでないとダメなはずだから、そこをどうするか...

EbN0 = 15 くらいまで上げないとoutputでクラスタリングした時のビット長が違ってきてしまう。
user_num = 3
m = 10
あたりのパラメータも変えてみてどうなるか、次で試してみる
