
今回のバージョンで、一通り完成した。

h の推定値が一直線になっていた原因は距離d をユーザ固定にしておらず、毎スロットごとに設定していたからであった。
デバッグすればすぐ分かることを...

h の係数に*10とかしているが、それは電力とか、距離による減衰と考えれば良い。

EMアルゴリズムの方もちゃんと作用していて、とりあえず完成という形になった。

あ、グラフでうまく色分けしておこうね。！！！！！！
-> しました！
でも、ラベルつけるところは手動(traialの数、user_numの数を変えたら変えないといけない)だから気をつけて、あと、h の推定値の実数のみで昇順で並べて、上から1~10,11~20,21~30でラベルをつけているから、たまに間違えてるよ。。。

したがって、
・復号アルゴリズムをちゃんと自分でできるfように理解していく。
・論文を書いていく。
・成果を出していく。
以上３つをしていく。


Encoder.mで generate_random_bits()を少し変更。
複数ユーザ間で同じ場合は除くことを実装
0 ~ self.B-1までのバイナリでの値(コメント内に格納中)をコマンドウィンドウで使えるようにした
使い方はEncoder.mのコメントにある


