前のバージョンDRAFT_0_1 では

普通にEMアルゴリズムでやろうと考え、
EMアルゴリズムのファイル sample2_em_algorithm.m を追加する
ユーザ数を固定する 符号ビットm = 8bit で k = user_num = 5 
tester.m :
K を消す。user_num を追加
whileや
save(file)を消す。

といった処理を追加した。

ただ、EMアルゴリズムを適用させるところで詰まっている。

今回は、
・EMアルゴリズムにデータを適用されることを考える。

sample2_em_algorithm.mの代わりに、em_algo.mを作成して、引数としてデータを渡せるようにした
2つのクラスで行なっていたEMアルゴリズムを3つのクラスでできるようにした。

程先生との会議で変更してしまったので、
ユーザ単位で固定のメッセージを送るようになっているので注意。



